table:
  name: workflow_triggers
  schema: public
object_relationships:
  - name: workflow
    using:
      foreign_key_constraint_on: workflow_id
insert_permissions:
  - role: user
    permission:
      check:
        workflow:
          organization:
            org_members:
              _and:
                - user_id:
                    _eq: X-Hasura-User-Id
                - _or:
                    - role:
                        _eq: owner
                    - role:
                        _eq: editor
      columns:
        - config
        - type
        - workflow_id
select_permissions:
  - role: user
    permission:
      columns:
        - config
        - created_at
        - id
        - type
        - workflow_id
      filter:
        workflow:
          organization:
            org_members:
              user_id:
                _eq: X-Hasura-User-Id
update_permissions:
  - role: user
    permission:
      check: null
      columns:
        - config
        - type
      filter:
        workflow:
          organization:
            org_members:
              _and:
                - user_id:
                    _eq: X-Hasura-User-Id
                - _or:
                    - role:
                        _eq: owner
                    - role:
                        _eq: editor
delete_permissions:
  - role: user
    permission:
      filter:
        workflow:
          organization:
            org_members:
              _and:
                - user_id:
                    _eq: X-Hasura-User-Id
                - _or:
                    - role:
                        _eq: owner
                    - role:
                        _eq: editor
